import 'package:flutter/material.dart';

class ChartMeasure extends StatelessWidget {
  const ChartMeasure({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
